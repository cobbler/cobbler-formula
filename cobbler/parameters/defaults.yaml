# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: cobbler
  rootgroup: root
  config: '/etc/cobbler'
  service:
    name: cobbler
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
